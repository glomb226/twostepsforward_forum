<p id="notice"><%= notice %></p>

<h1>Listing Links</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Supplychain</th>
      <th>Title</th>
      <th>Body</th>
      <th>Complete</th>
      <th>Eta</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @links.each do |link| %>
      <tr>
        <td><%= link.user_id %></td>
        <td><%= link.supplychain_id %></td>
        <td><%= link.title %></td>
        <td><%= link.body %></td>
        <td><%= link.complete %></td>
        <td><%= link.eta %></td>
        <td><%= link_to 'Show', link %></td>
        <td><%= link_to 'Edit', edit_link_path(link) %></td>
        <td><%= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Link', new_link_path %>
