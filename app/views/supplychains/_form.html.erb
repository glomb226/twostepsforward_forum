<%= form_for(@supplychain) do |f| %>
  <% if @supplychain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplychain.errors.count, "error") %> prohibited this supplychain from being saved:</h2>

      <ul>
      <% @supplychain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :complete %><br>
    <%= f.check_box :complete %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
